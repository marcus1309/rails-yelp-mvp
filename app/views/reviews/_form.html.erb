<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :content, "content"%>
  <%= f.text_field :content %>
  <%= f.label :rating, "rating"%>
  <%= f.text_field :rating %>
<%= f.submit %>
<% end %>
